<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yy.spring.dao.UserRoleDao">
<select id="selectByUid" resultType="UserRole" parameterType="int">
SELECT id,uid,rid FROM `userrole` where uid =#{uid}
</select>
</mapper>